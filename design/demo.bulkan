# when specialization works propertly, this should calculate the nth fib.

fib(n) = fib(n - 2) + fib(n - 1);
# fib(1) = 1;
# fib(0) = 0;

main() = 
	let n = 5
	let g = n > 1 == 5 < 2
	log 'the $n-nth fib is: $n';
